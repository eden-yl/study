# 256 color 모드로 X 윈도 실행
startx -- -depth 8

# 하이컬러(16비트) 모드로 X 윈도 실행
startx -- -depth 16

# 두 번째 X 윈도 실행(CentOS 7에서는 옵션 없이 startx도 가능)
startx -- :1

# 현재 화면의 크기 정보 출력
xrandr

# 화면의 크기(해상도)를 1024x768로 변경
xrandr -s 1024x768
